import React,{createContext,useContext,useEffect,useState}from'react';import{api}from'../services/api';const C=createContext();export const useAuth=()=>useContext(C);export function AuthProvider({children}){const[user,setUser]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{if(!localStorage.getItem('daybalance_token'))return setLoading(false);api.me().then(setUser).catch(()=>localStorage.removeItem('daybalance_token')).finally(()=>setLoading(false))},[]);const finish=d=>{localStorage.setItem('daybalance_token',d.token);setUser(d.user)};return <C.Provider value={{user,loading,login:async b=>finish(await api.login(b)),register:async b=>finish(await api.register(b)),logout:()=>{localStorage.removeItem('daybalance_token');setUser(null)}}}>{children}</C.Provider>}