export async function getWeather(city){if(!city.trim())throw new Error('Enter a city.');const g=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(city)}&count=1&language=en&format=json`).then(r=>r.json());if(!g.results?.length)throw new Error('City not found.');const place=g.results[0];const d=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${place.latitude}&longitude=${place.longitude}&current=temperature_2m,precipitation,weather_code,wind_speed_10m`).then(r=>r.json());return {place,current:d.current,suggestion:d.current.precipitation>0?'Indoor habit day: try reading, stretching or meditation.':'Good conditions for an outdoor walk habit.'}}