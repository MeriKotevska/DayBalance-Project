export const titleCase=s=>String(s||'').replace(/\b\w/g,c=>c.toUpperCase());export const formatDate=d=>new Intl.DateTimeFormat(undefined,{month:'short',day:'numeric',year:'numeric'}).format(new Date(d));export const formatDuration=n=>`${n||0} min`;export const timeOfDayLabel=s=>s==='anytime'?'Any time':titleCase(s);export const difficultyLabel=s=>`${titleCase(s)} level`;export const filterBySearch=(a,q,fields=['name'])=>!q?a:a.filter(x=>fields.some(f=>String(x[f]||'').toLowerCase().includes(q.toLowerCase())));export const completionPercentage=(done,total)=>total?Math.round(done/total*100):0;