import React,{useEffect,useState}from'react';import{api}from'../services/api';import HabitCard from'../components/HabitCard';import{filterBySearch}from'../utils/transformers';export default function Explore(){const[h,setH]=useState([]),[q,setQ]=useState('');useEffect(()=>{api.habits().then(setH)},[]);return <div className="container"><div className="page-intro"><span className="eyebrow">Habit library</span><h1 className="display">Explore healthy ideas</h1><p>Search real habits stored in DayBalance.</p></div><div className="toolbar"><div className="search-wrap"><input className="search-input" placeholder="Search habits" value={q} onChange={e=>setQ(e.target.value)}/></div></div><div className="library-grid">{filterBySearch(h,q,['name','description']).map(x=><HabitCard key={x._id} habit={x}/>)}</div></div>}