import React,{useEffect,useState}from'react';import{Bar}from'react-chartjs-2';import{Chart as ChartJS,CategoryScale,LinearScale,BarElement,Tooltip,Legend}from'chart.js';import{api}from'../services/api';import{formatDate}from'../utils/transformers';ChartJS.register(CategoryScale,LinearScale,BarElement,Tooltip,Legend);export default function Progress(){const[c,setC]=useState([]);useEffect(()=>{api.completions().then(setC)},[]);const days=[...Array(7)].map((_,i)=>{const d=new Date();d.setDate(d.getDate()-(6-i));return d}),counts=days.map(d=>c.filter(x=>x.dayKey===d.toISOString().slice(0,10)&&x.completed).length);return <div className="container"><div className="page-intro"><span className="eyebrow">Real completion data</span><h1 className="display">Your progress</h1></div><div className="surface chart-wrap">{c.length?<Bar data={{labels:days.map(formatDate),datasets:[{label:'Completed habits',data:counts}]}} options={{responsive:true,plugins:{legend:{display:false}},scales:{y:{beginAtZero:true,ticks:{precision:0}}}}}/>:<div className="empty">Complete a habit to see your chart.</div>}</div></div>}