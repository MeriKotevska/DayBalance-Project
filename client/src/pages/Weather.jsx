import React,{useState}from'react';import{getWeather}from'../services/weather';import{ErrorBox}from'../components/Layout';export default function Weather(){const[city,setCity]=useState('Bitola'),[d,setD]=useState(null),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function go(e){e.preventDefault();setLoading(true);setError('');try{setD(await getWeather(city))}catch(e){setError(e.message)}finally{setLoading(false)}}return <div className="container"><div className="page-intro"><span className="eyebrow">External service</span><h1 className="display">Weather & your habits</h1><p>Live data from Open-Meteo can help you adapt outdoor routines.</p></div><form className="toolbar" onSubmit={go}><input className="search-input" value={city} onChange={e=>setCity(e.target.value)} required placeholder="City"/><button className="btn btn-primary">Check weather</button></form><ErrorBox error={error}/>{loading&&<p>Loading…</p>}{d&&<div className="surface weather-result"><span className="eyebrow">{d.place.name}, {d.place.country}</span><div className="temperature-big">{d.current.temperature_2m}°C</div><p>Wind {d.current.wind_speed_10m} km/h · precipitation {d.current.precipitation} mm</p><div className="notice">{d.suggestion}</div></div>}</div>}