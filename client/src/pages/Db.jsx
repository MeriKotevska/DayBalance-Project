import React,{useState}from'react';import{api}from'../services/api';export default function Db(){const[msg,setMsg]=useState('');async function go(a){try{const r=await api.db(a);setMsg(r.message)}catch(e){setMsg(e.message)}}return <div className="container"><div className="page-intro"><span className="eyebrow">Admin utility</span><h1 className="display">Database reset & seed</h1><p>Dangerous database actions are protected by admin JWT authorization.</p></div><div className="surface danger-zone" style={{padding:25}}><div className="actions"><button className="btn btn-danger" onClick={()=>go('reset')}>Reset Database</button><button className="btn btn-secondary" onClick={()=>go('seed')}>Seed Initial Data</button><button className="btn btn-primary" onClick={()=>go('reset-and-seed')}>Reset & Seed</button></div>{msg&&<div className="notice">{msg}</div>}</div></div>}