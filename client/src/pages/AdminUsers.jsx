import React,{useEffect,useState}from'react';import{api}from'../services/api';import{formatDate}from'../utils/transformers';import{Modal}from'../components/Layout';export default function AdminUsers(){const[u,setU]=useState([]),[del,setDel]=useState(null);const load=()=>api.users().then(setU);useEffect(()=>{load()},[]);async function remove(){await api.deleteUser(del._id);setDel(null);load()}return <div className="container"><div className="page-intro"><h1 className="display">Users</h1></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Joined</th><th/></tr></thead><tbody>{u.map(x=><tr key={x._id}><td>{x.name}</td><td>{x.email}</td><td>{x.role}</td><td>{formatDate(x.createdAt)}</td><td><button className="btn btn-quiet" onClick={()=>setDel(x)}>Delete</button></td></tr>)}</tbody></table></div><Modal open={!!del} title="Delete user?" onClose={()=>setDel(null)} onConfirm={remove}>This permanently deletes {del?.email}.</Modal></div>}