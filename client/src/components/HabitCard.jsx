import React from'react';import{Link}from'react-router-dom';import{formatDuration,timeOfDayLabel,difficultyLabel}from'../utils/transformers';export default function HabitCard({habit,personal=false}){return <article className="habit-card"><span className="tag">{habit.category?.name||'Habit'}</span><h3>{habit.name}</h3><p>{habit.description}</p><div className="meta-line"><span>{timeOfDayLabel(habit.timeOfDay)} · {formatDuration(habit.duration)}</span><span>{difficultyLabel(habit.difficulty)}</span></div><Link className="text-link" to={personal?`/my-habits/${habit._id}`:`/habits/${habit._id}`}>View details →</Link></article>}