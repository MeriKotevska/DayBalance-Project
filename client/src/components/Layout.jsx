import React from'react';import{Link,NavLink}from'react-router-dom';import{useAuth}from'../context/AuthContext';export function Layout({children}){const{user,logout}=useAuth();return <><header className="site-header"><div className="container"><Link className="brand" to="/"><span className="brand-mark"/>DayBalance</Link><nav className="main-nav"><NavLink to="/explore">Explore</NavLink><NavLink to="/tips">Tips</NavLink><NavLink to="/weather">Weather</NavLink>{user&&<><NavLink to="/dashboard">Dashboard</NavLink><NavLink to="/my-habits">My Habits</NavLink><NavLink to="/progress">Progress</NavLink></>}{user?.role==='admin'&&<NavLink to="/admin">Admin</NavLink>}</nav><div className="nav-actions">{user?<><span className="small">{user.name}</span><button className="btn btn-secondary" onClick={logout}>Logout</button></>:<><Link to="/login" className="btn btn-quiet">Log in</Link><Link to="/register" className="btn btn-primary">Start free</Link></>}</div></div></header><main>{children}</main><footer className="site-footer"><div className="container"><span>DayBalance · small habits, steady progress</span><span>University Web Programming Project</span></div></footer></>}
export const Loading=()=> <div className="container section">Loading…</div>;export const ErrorBox=({error})=>error?<div className="error-hint">{error}</div>:null;export function Modal({open,title,children,onConfirm,onClose}){if(!open)return null;return <div className="modal-backdrop"><div className="modal-box"><h3>{title}</h3><p>{children}</p><div className="hero-actions"><button className="btn btn-danger" onClick={onConfirm}>Confirm</button><button className="btn btn-secondary" onClick={onClose}>Cancel</button></div></div></div>}