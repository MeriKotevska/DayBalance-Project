const express=require('express');const r=express.Router();
r.get('/health',(req,res)=>res.json({status:'ok',timestamp:new Date().toISOString()}));
r.use('/auth',require('./authRoutes'));r.use('/users',require('./userRoutes'));r.use('/categories',require('./categoryRoutes'));r.use('/habits',require('./habitRoutes'));r.use('/completions',require('./completionRoutes'));r.use('/tips',require('./tipRoutes'));r.use('/activity',require('./activityRoutes'));r.use('/db',require('./dbRoutes'));module.exports=r;